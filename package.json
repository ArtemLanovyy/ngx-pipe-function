{
  "name": "ngx-pipe-function",
  "version": "1.0.0",
  "description": "Angular pipe for memoize function execution in the template",
  "scripts": {
    "test": "jest"
  },
  "maintainers": [
    "Artem Lanovyy"
  ],
  "jest": {
    "globals": {
      "ts-jest": {
        "tsConfig": "<rootDir>/tsconfig.spec.json"
      }
    },
    "preset": "jest-preset-angular",
    "setupTestFrameworkScriptFile": "<rootDir>/src/setupJest.ts"
  },
  "peerDependencies": {
    "@angular/common": "^7.1.0",
    "@angular/core": "^7.1.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "@types/node": "^10.12.18",
    "jest": "^23.6.0",
    "jest-preset-angular": "^6.0.2",
    "ts-jest": "^23.10.5",
    "typescript": "^2.7.2"
  }
}
